from setuptools import setup, find_packages

setup(
    name="neuromorphic",
    version="0.1.0",
    description="An autonomous neuromorphic computing primitive",
    author="Your Name",
    packages=find_packages(),
    install_requires=[],
)
